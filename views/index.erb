<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Todolist</title>

  <link rel="stylesheet" href="bootstrap.min.css">
</head>
<body class="container p-5 bg-white">
  <h1 class="text-center">My Todo List</h1>

  <form action="/tasks" method="POST">
    <div class="row">
      <div class="col-9">
        <input class="form-control" type="text" name="name" placeholder="New task" required>
      </div>
      <div class="col-3">
        <button class="form-control" type="submit">Add Task</button>
      </div>
    </div>
  </form>

  <div class="py-5">
    <h2 class="text-center">Tasks</h2>
    <% @tasks.each do |item| %>
    <div class="card my-4">
      <div class="card-body">
        <div class="d-flex justify-content-between">
          <span><%= item.name %></span> 
          <span><%= item.description %></span> 
          <div class="form-check">
            <input class="form-check-input" type="checkbox" value="<%= item.done %>" id="flexCheckDefault" readonly disabled>
          </div>
        </div>
      </div>
    </div>
    <% end %>
  </div>
</body>
</html>
